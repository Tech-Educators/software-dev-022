import Image from "next/image";

// this component is called home - but it lives in a file called page.js
export default function Home() {
  return (
    <div>
      <h2>This is kinda just like normal react!</h2>
    </div>
  );
}
